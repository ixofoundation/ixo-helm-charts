apiVersion: v1
kind: Service
metadata:
  name: {{ include "sygnal.fullname" . }}
  labels:
    {{- include "sygnal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sygnal.selectorLabels" . | nindent 4 }}

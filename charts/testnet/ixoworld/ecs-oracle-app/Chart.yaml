apiVersion: v2
name: ecs-oracle
description: A Helm chart for Kubernetes
type: application
version: 0.0.42
appVersion: 2.4.6
annotations:
  artifacthub.io/changes: |
    - kind: changed
      description: Add startupProbe so slow boots survive the zero-delay liveness probe

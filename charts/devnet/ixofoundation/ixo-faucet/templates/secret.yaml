apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ixo-faucet.fullname" . }}
  labels:
    {{- include "ixo-faucet.labels" . | nindent 4 }}
type: Opaque
data:
{{- range $key, $value := .Values.envSecrets }}
  {{ $key }}: {{ $value | b64enc | default "" }}
{{- end }}

apiVersion: v2
name: memory-engine-graphiti
description: Memory engine (memory-engine-ts) — single service serving REST + MCP on one port
type: application
version: 0.1.0
appVersion: feat-cognee-ts-service-144579e
annotations:
  artifacthub.io/changes: |
    - kind: changed
      description: Replace the python worker/api-server/mcp-server Deployment and the matrix-bridge-api with a single memory-engine-ts Deployment on its own PVC; legacy workloads scaled to 0 with their PVCs retained for data migration
